#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.sync_with_stdio(0);
    cin.tie(0);

    int n, m, r, c;

    cin >> n >> m >> r >> c;

// i think i finally see it, just set all the top rows to the palindromes and all the left rows to palindromes, setting them all as one character, then just fill in the others using another character
    // the above solution doesn't work by the way
    // but we can do something similar and only change one column to have different values, allowing
    // yo i have no clue
}