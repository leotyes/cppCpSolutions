#include <bits/stdc++.h>
using namespace std;
// you can remove a lot of edges because it will already be covered, only keep adjacent edges and store them as nodes, start from the minimum edge adjacent to origin node 1
int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int n, m;
    cin >> n >> m;
}