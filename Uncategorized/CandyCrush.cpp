#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.sync_with_stdio(0);
    cin.tie(0);

    int k;
    string s;
    stack<pair<char, int>> st;

    cin >> k >> s;

    for (int i = 1; i < s.length(); i++) {
        if (s[i] == s[i - 1]) {

        }
    }

    for (int i = 0; i < s.length(); i++) {
        st.emplace(s[i], )
    }
}