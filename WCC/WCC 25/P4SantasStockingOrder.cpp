#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n;

    cin >> n;

    vector<int> va(n);

    for (int i = 0; i < n; i++) {
        cin >> va[i];
    }

    for (int i = 0; i < n; i++) {
        if (va[i] < va[i + 1]) {

        }
    }
}