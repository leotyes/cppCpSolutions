#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int p, s;

    cin >> p >> s;

    if (p >= s) {
        cout << "YES";
    } else {
        cout << "NO";
    }
}