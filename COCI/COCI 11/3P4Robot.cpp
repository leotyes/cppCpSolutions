#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n, m;

    cin >> n >> m;

    vector<pair<int, int>> vc(n);

    for (int i = 0; i < n; i++) {
        int x, y;
        cin >> x >> y;
        vc.emplace_back(x, y);
    }

    string s;

    cin >> s;

    for (int i = 0; i < s.length(); i++) {

    }
}