#include <iostream>
#include <vector>
using namespace std;

int main() {
    cin.sync_with_stdio(0);
    cin.tie(0);
    int n, k;
    cin >> n >> k;
    vector<int> hi(n);
    for (int i = 0; i < n; i++) cin >> hi[i];
    sort(hi.begin(), hi.end(), )
}